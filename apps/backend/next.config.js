/** @type {import('next').NextConfig} */
const nextConfig = {
  // Use Node.js runtime for all API routes by default
  experimental: {
    // serverComponentsExternalPackages: [],
  },
};

module.exports = nextConfig;
