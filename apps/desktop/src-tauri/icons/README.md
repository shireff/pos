# Icons Directory

Place the following icon files here before bundling:

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

You can generate them using:

```
npm run tauri icon path/to/your-logo.png
```
